<div class="container py-5">
  <div class="row">
    <div class="col-12 col-md-12">
      <div class="bg-white rounded p-5 shadow">
        <h1 class="mb-5">Chambres disponibles</h1>

        <div class="row">
            <% @rooms.each do |room| %>
              <div class="co-12 col-sm-4 col-md-4">
                <%= link_to room_path(room), class: "btn btn-dark m-1" do %>
                    <p><%= room.price_per_night %> €</p>
                <% end %>
              </div>
            <% end %>
          </div>

        <%= link_to 'Retour', root_path, class: 'btn btn-primary m-1' %>
      </div>
    </div>
  </div>
</div>
